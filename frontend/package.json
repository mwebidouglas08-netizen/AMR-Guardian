{
  "name": "amr-guardian",
  "version": "1.0.0",
  "description": "AMR Guardian - AI-Powered AMR Intelligence Platform",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "install-backend": "npm install --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
